[tools]
bun = "latest"
go = "1.24.4"
node = "22.15.1"
python = "3.12"
ruby = "3.4.4"
# rust/zig build the serie and herdr forks; zig pinned exactly — the vendored
# libghostty-vt build breaks on 0.16 (herdrdev/herdr#285). 0.15.2 cannot link
# the macOS 26 SDK, which herdr-fork-update shims xcrun around.
rust = "1.96.1"
uv = "latest"
zig = "0.15.2"
